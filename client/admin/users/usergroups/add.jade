#content
  != self.partial('@admin.core.blocks.sidebar_menu', { apiPath: self.apiPath, menu: self.menus.admin })

  .main
    != self.partial('@admin.core.blocks.breadcrumbs', { title: self.head.title, apiPath: self.apiPath, menu: self.menus.admin, })

    form.form-horizontal(data-on-submit='admin.users.usergroups.add')
      - var _input_help_text

      legend
        h1 #{self.head.title}

      - _input_help_text = self.t('short_name_help')

      .cg-head

        .control-group
          label.control-label(for='short_name') #{self.t('short_name_title')}
          .controls
            .row-fluid
              input(type="text", name='short_name')
              span.help-block #{_input_help_text}

        != self.partial('@admin.users.blocks.parent_group_select', { usergroups: self.usergroups, current: self.current })
        != self.partial('@admin.users.blocks.settings_filter')


      - each category, category_name in self.settings_categories

        legend #{self.t('@admin.setting.category.' + category_name)}
        != self.partial('@admin.users.blocks.settings_category', { name: category_name, data: category })


      .form-actions
        button.btn.btn-primary.btn-large(type='submit') #{self.t('create')}
