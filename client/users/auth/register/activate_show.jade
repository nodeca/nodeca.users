#content.row
  .col-sm-8.col-sm-offset-2.col-lg-6.col-lg-offset-3
    .panel.panel-default
      .panel-body
        if self.success
          = self.t('message_success')
        else
          = self.t('message_invalid_token')
