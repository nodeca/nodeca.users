#content.row
  .col-sm-8.col-sm-offset-2.col-lg-6.col-lg-offset-3
    .panel.panel-default
      .panel-body
        if self.success
          = self.t('success')
        else
          = self.t('invalid_token')
