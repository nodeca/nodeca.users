{
  "name": "nodeca.users",
  "description": "Nodeca users app",
  "version": "0.0.1",
  "keywords": [
    "nodeca",
    "users"
  ],
  "license": "MIT",
  "repository": "nodeca/nodeca.users",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
