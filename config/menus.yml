menus:
  common:
    navbar:
      tracker:
        to: users.tracker
        autoselect: false
        priority: 10
  admin:
    navbar:
      users:
        to: admin.users.usergroups.index
        submenu:
          usergroups:
            to: admin.users.usergroups.index
          members:
            to: admin.users.members.search
