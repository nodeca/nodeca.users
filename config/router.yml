router:
  map:
    users.profile:
      "/profile": ~

    users.member:
      "/member{id}":
        id: /\d+/

    admin.users.index:
      "/users": ~


    users.auth.login.view:
      "/auth/login": ~

    users.auth.logout:
      "/auth/logout": ~

    users.auth.register.view:
      "/auth/reg": ~

    users.auth.register.success:
      "/auth/reg/done": ~

    admin.users.usergroups.index:
      "/usergroups": ~

    admin.users.usergroups.new:
      "/usergroups/new": ~

    admin.users.usergroups.show:
      "/usergroups/show/{short_name}":
        short_name: /\w+/
