i18n:
  en-US:
    admin:
      core:
        group_names:
          register: Registration

        category_names:
          register_common: Common

        setting_names:
          general_security_policy: Security policy

          register_user_initial_group: Group after registration

          register_user_validated_group: Group after activation

        setting_values:
          general_security_policy:
            https_for_registered: HTTPS for registered users only
            https_only: HTTPS for all
            http_only: HTTP for all
