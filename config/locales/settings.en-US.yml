i18n:
  en-US:
    admin:
      core:
        group_names:
          register: Registration
          auth: Authentication

        category_names:
          register_common: Common
          auth_common: Common

        setting_names:
          register_user_initial_group: User initial group
          register_user_initial_group_help: Group for newly registered users.

          register_user_validated_group: Validated user group
          register_user_validated_group_help: Group to set after user confirmed
            registration email address.

          auth_logout_redirect_url: Logout redirect
          auth_logout_redirect_url_help: URL to redirect to when user logs out.
